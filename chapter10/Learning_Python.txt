Learning Python

In C you can use print to Print the values to a stream, or to sys.stdout by default.
In C Comments starts with a #, and Python will ignore them.
In C there are no command for declaring a variable.
In C a variable is created the moment you first assign a value to it.
In C there are three numeric types in Python: int, float, complex. 